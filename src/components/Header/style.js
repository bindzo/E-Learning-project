import { createStyles } from "@material-ui/core";

const style = (theme) =>
  createStyles({
    logo: {
      marginRight: 20
    },
    header: {
        height: 70
    },
    button: {
        padding: '10px 30px',
        marginRight: 10
    },
    about:{
        marginRight: 20
    }
  });

export default style;
